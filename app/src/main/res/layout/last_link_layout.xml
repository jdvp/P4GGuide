<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal|top"
    android:gravity="center_horizontal|top">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/tv"
        android:id="@+id/link_tv"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/social_link_aeon"
        android:id="@+id/link_person"
        android:layout_alignStart="@+id/link_tv"
        android:layout_alignRight="@+id/link_tv"
        android:layout_alignEnd="@+id/link_tv"
        android:layout_alignLeft="@+id/link_tv"
        />
</RelativeLayout>